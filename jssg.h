#ifndef JSSG_H
#define JSSG_H

/* void generate_article(char* header, char* footer, char* article_path, char* fname) */
void read_file(char *file_buf, char* path, int count);
void generate_article(char* header, char* footer, char* fname);
#endif

	

